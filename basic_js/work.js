

const work = function(x,y) {
console.log(Math.min(x,y));
}
work(2,3);
-----------------------------------------------------

function isEven(x) {
 if (x % 2 === 0) {
   console.log(true);
 } else {
   console.log(false);
 }
}

isEven(-1);
-----------------------------------------------------

function isEvenTwo(x) {
  const 0 = even;
  const 1 = odd;
} if (0)


//account for neg sign in -1//
